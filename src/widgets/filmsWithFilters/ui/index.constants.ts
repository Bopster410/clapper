export const DEFAULT_APP_BAR_HEIGHT = 64;
export const DEFAULT_TOP_PADDING = 16;

export const DEFAULT_MIN_RATING = 0;
export const DEFAULT_MAX_RATING = 10;

export const DEFAULT_MIN_YEAR = 1990;
export const DEFAULT_MAX_YEAR = new Date(Date.now()).getUTCFullYear();
