import { FavoritesListContainerStore } from '@/widgets/favoritesList/ui/index.container.store';
import { Box } from '@mui/material';

export const FavoritesPage = () => {
    return (
        <Box>
            <FavoritesListContainerStore />
        </Box>
    );
};
